毎週土曜日08:15 日本時間（Asia/Tokyo）にEAユーザー向け週次レポートを生成・納品する。最初に`/home/ubuntu/skills/ea-weekly-report/SKILL.md`と`references/future_policy.md`を読み、EA特化型の正規構成・一次情報・根拠台帳・監査手順に従う。適用判定は`report.target_start`で行い、2026-08-03以降だけ将来ポリシーを適用する。対象週が2026-08-03より前の既存成果物は履歴アーカイブであり、再生成、再ラベル、上書き、又は新ポリシー適合判定をしない。旧「SEC 01」「SEC 02」を含む市況サマリー・週次相場展望・通貨ペア価格・指数価格・予想記事は掲載禁止とする。本文は「EA運用タイムテーブル」「来週の注目イベント（日本時間）」「EAリスク管理ポイント」の3ブロックのみで構成し、日本時間の日付別親カードと時刻順の子カードを使う。イベント名・日時・対象期間・公式結果は一次情報で照合し、前回値・改定値・市場予想はfxshihyo.com又はみんかぶFXの公開カレンダー値を使用できる。この民間カレンダー値には提供元・URL・確認時刻を根拠台帳に記録し、読者向けには「民間カレンダー値」と明示する。重要イベントの新規エントリー停止は予定時刻の4〜12時間前を目安とし、公式に時刻未公表なら推測時刻を置かず公式発表確認まで停止を継続する。2026-08-03以降は`ea_weekly_policy`の固定値と`ea-only-v3`の将来ポリシー属性を必須とし、読者向け可視テキストに`JST`を出さない。生成後は`render_ea_weekly_correct_design_new.py`、`build_preview.py`、`build_wordpress.py`、`validate_daily_event_layout.py`、`validate_ea_weekly_scope.py`、`validate_mobile_header.py`、`publish_gate.py`を実行する。いずれかがFAIL、ERROR、WARNING、モバイル幅検査不合格、`PUBLISH_READY`以外、又は将来ポリシー属性不一致ならHOLDとし、プレビューURL・完成HTMLを納品しない。