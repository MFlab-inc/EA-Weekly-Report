# 一時ワークフロー（2026-09-26）: gb_boe_calendar実装のfixture採取（scripts/phase1/source-recon-s.mjs）。
# 確認完了後、このファイルと.boe-calendar-recon-triggerは削除する
# （source-recon-s.mjs自体は監査記録として残す）。
name: boe-calendar-recon
on:
  push:
    paths:
      - '.boe-calendar-recon-trigger'
jobs:
  recon:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with: { node-version: '20' }
      - run: node scripts/phase1/source-recon-s.mjs
