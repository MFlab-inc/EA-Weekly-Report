# task #41ライブ検証用の一時ワークフロー（しょうさん指示2026-08-15）。
# scripts/phase1/source-recon-h3.mjsを実ネットワーク環境で実行し、ジョブログから結果を回収する。
# 検証完了後は削除すること（task #38のpipeline-dry-run.ymlと同じ運用）。
name: source-recon-h-dry-run
on:
  workflow_dispatch: {}
  push:
    branches:
      - claude/ea-weekly-report-automation-lw6cio
    paths:
      - '.github/workflows/source-recon-h-dry-run.yml'
      - 'scripts/phase1/source-recon-h3.mjs'
permissions:
  contents: read
jobs:
  recon:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - name: source-recon-h3
        run: node scripts/phase1/source-recon-h3.mjs
