# SPEC.md — EAユーザー向け週次レポート 仕様正本 v2.1

確定日: 2026-08-13（v2.0）／ 最終改訂: 2026-08-14（v2.1） ／ 本書は旧6ファイル（legacy-prompt / 表示・命名ポリシーv1.0 / 2スキル / サンプル / カレンダー）と再構築計画書（docs/rebuild-plan_v0.3.md）の全決定を統合した**実装の唯一の正**である。旧文書と矛盾する場合は本書が優先する。旧版からの主な変更: 停止目安4〜12時間前の復活（§5）、出典表記の変更（§7）、レイアウトv4（§6）、みんかぶ＋FFハイブリッド収集（§3、**v2.1で撤回・後述**）。

## 変更履歴

- **v2.1（2026-08-14）**: Phase 0実測（`docs/phase0-findings.md`）を経て、しょうさんが**みんかぶFXを収集ソースとして恒久除外**と決定（許諾問い合わせは行わない）。影響範囲:
  - §3 データソース仕様: 全面改訂。統計指標も発言・イベント系と同様に**公式発表元（統計局・中銀等）からの収集を本則**とし、Forex Factory週次フィードは検出の主ソースから「存在・時刻の突合＋月曜事後突合」用途に後退
  - §4.2 命名: 統計指標の名称は新設 `config/event-names.json`（英名→日本語正規名の辞書）で解決。未登録はWARN＋掲載除外（推測命名は引き続き禁止）
  - §4.3 重要度: みんかぶ★マッピングを廃止。`config/importance-rules.json` の自前規則（event kind × country）を唯一の重要度源に昇格
  - §7 出典・表示: `show_prev_forecast` の運用既定値を恒久 `false` に確定（トグル機構は実装上維持するが、みんかぶ許諾を理由にON化することはない）
  - §11 未決事項: みんかぶ関連の未決事項をクローズ（詳細は `docs/open-questions.md` Q1〜Q5の決定内容）
  - 検収基準（§本書冒頭に相当するHANDOFF.md §1）は不変: 既刊2週の全29イベントを対象週指定で100%捕捉することに変わりはなく、公式ソース収集でこれを満たす

## §1 目的・配信概要

- 目的: EA（自動売買）ユーザーへ、対象週（翌週月〜金）の重要経済イベントと**新規エントリー停止の開始目安**を配信する
- 配信: 毎週土曜 08:06 JST自動生成 → しょうさんがWordPressへ貼り付け掲載
- 読者環境: スマートフォン中心。**上下スクロールのみで閲覧完結**（横スクロール要素・タブ・折りたたみ禁止）
- 本レポートは情報提供であり売買助言ではない（§7の免責を必ず掲載）

## §2 パイプライン全体

GitHub Actions（`weekly.yml`）で以下を順次実行。全ステップ成功時のみ配信物をコミットする（フェールクローズ）。

```
collect（公式ソース群＋FF取得・キャッシュ保存）
→ normalize（正規化・命名・重要度付与・国/通貨付与）
→ ledger（根拠台帳JSON生成: data/ledger/YYYY-MM-DD.json）
→ check（鮮度・クロス突合・定例欠落・ポリシーlint）
→ render（output断片HTML＋docs/preview生成）
→ validate（移植5本＋新設3本）
→ gate（PUBLISH_READY判定 → コミット or HOLD）
```

- 対象週の定義: 実行日（土曜）の翌月曜〜金曜。日付計算はJST基準（`reference/ea-weekly-report-skill/scripts/get_next_week_dates.py` の仕様を踏襲しNode実装）
- HOLD時: `hold-report.json` に理由列挙・runを失敗させ・`output/` は更新しない。プレビューには「今週分は保留」を表示

## §3 データソース仕様

**2026-08-14決定: みんかぶFXは収集ソースとして恒久不使用**（許諾問い合わせを行わない方針。経緯は `docs/open-questions.md` Q1・Q3・Q4）。統計指標・発言系のいずれも**公式発表元からの収集を本則**とする。Forex Factory週次フィードは検出の主ソースではなく、**存在・時刻の突合＋月曜事後突合**に用途を限定する。

### 3.1 統計指標・発言系の収集（公式ソース本則）

- 対象週の候補イベントは、カテゴリごとの公式発表元チェッカー（§3.2の対象元リスト）から機械取得する
- 名称: 統計指標=`config/event-names.json`（英名→日本語正規名の辞書。未登録はWARN＋掲載除外。推測命名は禁止）／発言・イベント系=§4.2の規則生成（`config/officials.json`）
- 数値（前回値・市場予想）: 統計局の多くは発表予定ページに前回値・予想値を掲載しないため、`show_prev_forecast=false`（恒久既定値。§7）で数値行は非掲載とし、読者へは民間カレンダー（fxshihyo・みんかぶFX等）へのリンク誘導とする
- チェッカー対象元の最小リストと実装順は実装計画（`docs/phase1-official-sources.md`）で管理する

### 3.2 Forex Factory週次フィード（存在・時刻の突合用）

- 用途: 公式ソースチェッカーで検出した候補の時刻確認・見落とし検知、および§3.3の月曜事後突合
- Phase 0実測（`docs/phase0-findings.md` 項目2）: `ff_calendar_thisweek.json`のみ提供、`nextweek.json`／`lastweek.json`は404。土曜生成時点のthisweekは現行週（対象週の前週）を指す可能性が高い（実測で最終確定）。**実測でthisweekが対象週を指すことが確認できた場合は、検出補助として復帰させてよい**（実測優先。しょうさん承認済み）
- レート制限: ネットワーク単位で5分2リクエスト。週1回・thisweek 1リクエストのみ取得し、キャッシュ優先・失敗時HOLD（§3.6）

### 3.3 月曜事後突合

- 対象週が実際の当該週になった月曜朝、`ff_calendar_thisweek.json`（このタイミングでは対象週分を指す）と配信済み台帳（`data/ledger/`）を突合する
- 相違ゼロ→静かに成功（ログのみ）。相違あり→`discrepancy-report.json`（イベント名・掲載時刻→正時刻の訂正リスト）を出力してrunを失敗させ通知。しょうさんがWordPress掲載済み記事を手動訂正できる形式とする
- cron: 月曜朝・人気分（0・5の倍数・15・30・45分）を回避。§3.2のレート制限を厳守

### 3.4 障害時のフォールバック・定例欠落検知（2026-08-14精緻化）

公式ソースチェッカーが失敗（構造変更・アクセス不能等）した場合の扱いは、そのソース担当のイベントが対象週に**発生することが見込まれているか**で分岐する（フェールクローズの精緻化。単純に「失敗=WARN」とはしない）:

- **見込みあり→HOLD**: 失敗したソースについて、次のいずれかが対象週の発生を示唆している場合
  - 年次スケジュールconfig型ソース（§3.5）で、そのソースの年次日程に対象週分のエントリが存在する
  - `config/importance-rules.json` の `recurring_checks`（例: 米雇用統計月の第1金曜、月央の米CPI等）が対象週に該当する
  - Forex Factory突合（§3.2）で、そのソース担当種別のイベント存在が示唆されている
- **見込みなし→WARN**: 上記いずれにも該当しない場合（そのソースが今週たまたま発生しない可能性が高い）。HOLDにはしない
- **判定不能→無条件HOLD**: 複数ソースが同時に失敗した場合（個別ソースの障害ではなくネットワーク・実行基盤側の広域障害の疑いがあるため、発生見込み判定を信用せずフェールクローズ側に倒す）
- 定例欠落検知: `recurring_checks` が対象週に見当たらない場合（フェッチ自体は成功したが期待イベントがない）はWARN（HOLDではない。祝日ずれがあるため）。★★★イベントの欠落が疑われる場合はこのWARNで可視化する

### 3.5 公式ソース機械チェッカー

- 対象: 統計指標・発言系の全担当元（政府統計機関・中央銀行・財務省・民間指数機関）。最小リストと実装優先順は `docs/phase1-official-sources.md`
- 民間指数機関（ISM・S&P Global系PMI・ADP・Ivey等、政府に代替元がないもの）は、発表元自身が年次で公表する固定リリース日程が多く、**週次スクレイピングでなく年次スケジュール（config管理）から対象週分を照合する方式**を優先検討する（サイト構造変化への耐性が高いため）。BOJ（毎年7月31日に翌年分公表）・MOF国債発行計画（毎年12月頃に翌年度分公表）・RBA理事会日程（毎年1回公表）等、政府・中銀の一部も同様の年次公表パターンを持つため、同方式を適用してよい

**年次スケジュールconfig型ソースの残量監視（2026-08-14追加）**: 年次スケジュールconfig型の各ソースについて、check段で「対象週から起算して4週先までの日程がconfigに存在するか」を確認する。存在しない場合はWARN（年次更新の失念防止）。運用手順（各機関の翌年日程公表後にconfigを更新する手順）は `docs/annual-schedule-maintenance.md` に記載する

**限界事項（2026-08-14明記・しょうさん指示）**: 公式ソース機械チェッカー（年次スケジュールconfig型・FRED/Census PFEI等の日付API問わず）の多くは**発表日程のみを提供し、発表時刻は含まない**。発表時刻は`config/official-sources.json`の指標別固定時刻（例: 米CPI/PPI/雇用統計=08:30 ET）に依存し、`scripts/lib/tz-convert.js`でJST換算する。この固定時刻が実際の発表慣行から変更された場合、日付ソース自体はその変化を検知できず、**唯一の検知手段は月曜FF事後突合（§3.3）**である。月曜突合は対象週が実際の当該週になった後の事後検証のため、当該週の配信物には誤った停止目安が反映されたまま出てしまう可能性がある。この限界はHANDOFF §1検収基準・本SPECのフェールクローズ設計の範囲内で許容する。

### 3.6 アクセス設計
取得間1500ms待機・User-Agent明示（`MFlab-EA-Weekly/1.0`）・週次実行のみ・取得生データを `data/cache/YYYYMMDD/` に保存し再実行時はキャッシュ優先（冪等）・失敗は3回リトライ後HOLD。**みんかぶ由来のデータ（生HTML・数値とも）はコミット物（`data/ledger`・`output`・`docs`プレビューを含む一切）に含めない**（`docs/open-questions.md` Q3）。FF取得は§3.2のレート制限を厳守。

## §4 イベント選定・命名・重要度

### 4.1 選定
- 掲載対象: ★★★（最重要）と★★（重要）。★以下は掲載しない
- 同時刻・同発表主体の関連イベントは**発表枠**として束ねる（例: CPI＋コアCPI、RBA声明＋四半期報告→14:30会見まで1枠）

### 4.2 命名（読者向けは日本語のみ）
- 統計指標: `config/event-names.json`（公式発表元/FFの英名→日本語正規名の辞書）を基準に、既刊レポートの表記慣行（例:【コア】【速報値】＆併記）へ正規化。英語名の露出は禁止。辞書未登録のイベントはWARN＋掲載除外（推測翻訳は行わない）
- 発言・イベント系（規則生成）:
  - 発言=「{人名}{役職}の発言」／会見=「{人名}{役職}の記者会見」／議会=「{人名}{役職}：{議会名}」
  - 中銀=「{中銀略称}政策金利＆声明発表」「{中銀略称}四半期金融政策報告」「日銀 金融政策決定会合における主な意見」等（reference/display-and-naming-policy_v1.0.md の形式を継承）
  - 入札（国別分岐。2026-08-14確定・既刊実例に基づく）:
    - 日本国債（JGB）=「{年限}利付国債（{発行年月}債）の入札」（既刊実例: 「10年利付国債（2026年8月債）の入札」「30年利付国債（2026年8月債）の入札」。発行年月＝発表元（財務省）の入札発表に記載の発行月）
    - 米国債=「米{年限}債入札」（例: 「米10年債入札」。既刊2週には実例なし。優先度Bソース米財務省=TreasuryDirectで検出）
- 人名は `config/officials.json` から解決。`verified: false` の役職は**人名を出さず役職のみ**で命名（例「FRB議長の発言」）— 誤記より情報減を選ぶ

### 4.3 重要度
- `config/importance-rules.json` の自前規則（event kind × country）を**唯一の重要度源**とする（みんかぶ★マッピングは廃止）。初期値はPhase 0で既刊2週の全29イベントと突合して校正済み（`docs/phase0-findings.md` 項目1）。中銀総裁の会見/議会証言=★★★、米国債入札=★★ 等。運用実測で継続調整する

## §5 停止目安の計算（本レポートの中核）

- 対象: **★★★イベントのみ**。停止開始目安 = [発表時刻−12時間, 発表時刻−4時間]
- 発表枠は枠内**最初**の発表時刻を基準に計算。再開確認は枠内**最後**のイベント終了後からと明記
- 窓が前日に跨る場合:「前日◯曜HH:MM〜」を明記。月曜早朝イベントで窓が日曜に跨る場合は「週明けの取引開始時点からの停止が目安」を付記
- 同日複数枠の停止タイムバー表示は窓の**和集合**を帯描画し、枠別の正確な時刻はテキスト併記
- 時刻「未定」・公式未公表のイベントは停止目安を計算しない（掲載時は「時刻未公表（停止目安の対象外）」）
- 表示文言・再開共通文・EA個別判断の免責は `config/report-policy.json` の確定文言を使用（改変禁止）
- 単体テスト必須ケース: 08:50月曜（週明け特例）／13:30+14:30枠（束ね）／同日2枠の和集合／21:30通常／未定除外／窓の分丸め（HH:MM維持・丸めなし）

## §6 レポート構成・表示仕様（レイアウトv4）

デザイン契約: `templates/design-mock_v1.2.html`。レンダラーはこの構造・インラインスタイルを再現し、データのみ差し替える。

1. **6.1 ヘッダー**: ブランド継承（EAロゴ・タイトル・作成日）。非固定（stickyにしない）
2. **6.2 ヒーロー**: 対象週・要約1行（事実列挙のみ: 「◯◯、◯◯を確認する週」形式）・主要イベントチップ3・件数行（★★★件数／発表枠数／対象日数）
3. **6.3 EA停止スケジュール**（新設・主役）: 日別カード×5。各カード=日付＋★★★件数、停止タイムバー（0〜24時、琥珀帯=停止開始目安、▲=発表時刻、目盛0/6/12/18/24）、枠別行=**[国名ピル][通貨コードピル]** ▲時刻 イベント名＋停止開始目安。凡例カードと再開・免責カードを前後に配置
4. **6.4 対象週の注目イベント（日本時間）**: 日付親カード→時刻順子カード。★★★=詳細カード（時刻・国ピル・名称・★・前回/予想[トグル]・定型コメント）、★★=1行圧縮。冒頭注記「前回値・市場予想は民間カレンダー値」（トグルON時のみ）
5. **6.5 EAリスク管理ポイント**: 3カード（重要イベントの前／発表中・発表直後／再開の判断）。文言はreport-policy.jsonの確定文言
6. **6.6 土日のBTC/USD EA運用ガイド**: 4カード。②は確認先リンク＋**3段階基準のバッジ縦積み**（バッジ幅62px固定）。内容・リンクは `config/btc-weekend-guide.json` から
7. **6.7 フッター**: 免責＋出典表記（§7）＋作成日・対象期間

**禁止事項（validate対象）**: 市況サマリー／相場展望・予想記事／通貨・指数の価格一覧／読者可視テキストの「JST」／英語イベント名／`html` `head` `body` `script` `style` `form` タグ・外部JS・トラッキング／見出しタグ（h1〜h6）／横スクロールを生む固定幅・nowrapの濫用。
**data-ea-*属性**: レポートルート（report-meta / layout-version="ea-only-v4" / target-start / section-count=4 / reader-time-term="日本時間" / halt-guidance="pre4to12h"）、日付グループ（date / date-event-count）、イベントカード（event-id / event-importance）。検証スクリプトはこの属性体系を前提に更新する。

## §7 出典・免責の確定文言

`config/report-policy.json` に全文を格納（レンダラーは転記のみ・改変禁止）。要点:
- 旧「発表日時は一次情報で確認し」「一次情報：◯◯」表記は**廃止**（自動化後は事実に反するため）
- 新出典表記: 民間カレンダーの公開情報に基づく旨＋機械検査の内容＋公式変更が未反映の可能性＋公式確認の推奨
- `show_prev_forecast`: **2026-08-14決定によりfalseを恒久既定値とする**（みんかぶ不使用に伴い、公式発表元からは前回値・市場予想が取得できないため）。トグル機構自体は実装上維持するが、みんかぶ許諾を理由にtrueへ切り替えることはない。false時は数値行を出さず、「前回値・市場予想は各カレンダーでご確認ください」＋読者向けリンク（fxshihyo・みんかぶへの**リンク掲載は可**）に自動切替
- 免責（投資助言でない・正確性非保証・自己責任・非ライセンス）は全文必須

## §8 検証ゲート

| 種別 | 検査 | 実装 |
|---|---|---|
| 移植 | 台帳静的検査／HTML×台帳照合／日付別カード監査／モバイル多幅（320/360/375/390/414）／PUBLISH_READY判定 | reference/ の5本をPythonのまま移植し、期待構造をv4へ更新 |
| 新設 | 鮮度検証（§3.1） | check段 |
| 新設 | クロス突合・定例欠落（§3.3） | check段 |
| 新設 | ポリシーlint＋リンク検査 | 禁止語・禁止タグ・免責/出典存在＋**外部リンクの到達性＆許可ドメインホワイトリスト照合**（`config/btc-weekend-guide.json` の `allowed_domains`。類似ドメイン事故の防止: rebuild-plan §13.4注意2） |

1件でも不合格→HOLD。フェールクローズをテストで担保（壊しデータでHOLDになることを確認）。

## §9 スケジュール（GitHub Actions）

- 本番cron `6 23 * * 5`（=土曜08:06 JST）／保険cron `41 23 * * 5`（=08:41 JST）／`workflow_dispatch`
- 冪等ガード: 当週分がPUBLISH_READY済みなら即終了（保険cron・手動再実行の二重生成防止）
- 分の選定はGitHub混雑帯・人気分回避の実測知見に基づく（EA-Risk-Monitor §19）。リポジトリはPublic前提（Actions実行制限・Pages無料利用のため）

## §10 成果物・受け渡し

- `output/ea-weekly-YYYYMMDD.html`（YYYYMMDD=対象週月曜）: WordPress貼り付け断片。Raw表示→全選択コピーで貼り付け可能な単一断片であること
- `docs/`（GitHub Pages）: プレビュー（断片をビューワーに内包）＋「配信可／保留」ステータス＋生成日時＋hold理由表示
- `data/ledger/`・`data/cache/`: 監査証跡。`data/calendar_weekly.json`: 正本の機械可読版

## §11 未決事項（実装と並行して解消）

1. ~~みんかぶ許諾~~ → **クローズ（2026-08-14）**: しょうさん決定によりみんかぶは恒久不使用。問い合わせ自体を行わない（`docs/open-questions.md` Q1・Q3・Q4）
2. FF翌週分の土曜可用性 → 実測中（`docs/phase0-findings.md` 項目2）。取得不可の場合は公式ソース収集が本則（決定済み・§3）
3. ~~`minkabu_star_map`~~ → **クローズ**: みんかぶ★マッピングは廃止。`config/importance-rules.json` の event kind × country 規則が唯一の重要度源（Phase 0校正済み。`docs/open-questions.md` Q2）
4. `officials.json` 全役職の照合 → **クローズ（2026-08-14）**: 全9役職 `verified: true` 更新済み（FRB議長=ウォーシュ・2026-05-22就任、RBNZ総裁=ブレマン・2025-12-01就任）
5. 公式ソースチェッカーの対象元最小リスト・実装順 → `docs/phase1-official-sources.md`（Phase 1着手事項）
